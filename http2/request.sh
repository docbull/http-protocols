VERSION=$1

go run client.go version ${VERSION}
sleep 5

go run client.go version ${VERSION}
sleep 5

go run client.go version ${VERSION}
sleep 5

go run client.go version ${VERSION}
sleep 5

go run client.go version ${VERSION}
sleep 5

go run client.go version ${VERSION}
